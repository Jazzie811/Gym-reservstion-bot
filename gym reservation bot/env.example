# Gym Reservation Bot Configuration
# Copy this file to .env and fill in your actual values
# OR set these as environment variables in your system

# Gym Credentials
GYM_USERNAME=your_username_here
GYM_PASSWORD=your_password_here
GYM_RESERVATION_URL=https://your-gym-reservation-url.com

# Optional: ChromeDriver path (if not in PATH)
# CHROMEDRIVER_PATH=/usr/local/bin/chromedriver

# Optional: Use GCP Secret Manager instead of environment variables
# USE_GCP_SECRETS=false
# GCP_PROJECT_ID=your-gcp-project-id

# Optional: CSS Selectors for website elements (customize for your gym's website)
# GYM_USERNAME_SELECTOR=input[name='username']
# GYM_PASSWORD_SELECTOR=input[name='password']
# GYM_LOGIN_BUTTON_SELECTOR=button[type='submit']
# GYM_POST_LOGIN_SELECTOR=.dashboard
# GYM_RESERVATION_PAGE_SELECTOR=a.reserve-link
# GYM_DATE_SELECTOR=.calendar-today
# GYM_TIME_SLOT_SELECTOR=button[data-time='11:00']
# GYM_SUBMIT_SELECTOR=button[type='submit']
# GYM_SUCCESS_SELECTOR=.success-message
# GYM_EXISTING_RESERVATION_SELECTOR=.existing-reservation

